{
  "$schema": "./node_modules/@biomejs/biome/configuration_schema.json",
  "vcs": { "enabled": true, "clientKind": "git", "useIgnoreFile": true },
  "files": { "includes": ["**", "!package-lock.json"] },
  "formatter": { "enabled": true, "indentStyle": "space", "indentWidth": 2, "lineWidth": 120 },
  "javascript": {
    "formatter": { "quoteStyle": "single" },
    "globals": ["game", "foundry", "CONFIG", "CONST", "Hooks", "ui", "canvas"]
  },
  "linter": {
    "enabled": true,
    "rules": {
      "preset": "recommended",
      "correctness": { "noUndeclaredVariables": "error", "noUnusedImports": "error", "noUnusedVariables": "error" }
    }
  },
  "overrides": [
    {
      "includes": ["src/core/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedGlobals": {
              "level": "error",
              "options": {
                "deniedGlobals": {
                  "game": "src/core is pure: pass the value in from the shell.",
                  "foundry": "src/core is pure: pass the value in from the shell.",
                  "CONFIG": "src/core is pure: pass the value in from the shell.",
                  "CONST": "src/core is pure: use MODES, which the shell checks against CONST at start-up.",
                  "Hooks": "src/core is pure: hooks belong to the shell.",
                  "ui": "src/core is pure: the interface belongs to the shell.",
                  "canvas": "src/core is pure: pass the value in from the shell."
                }
              }
            }
          }
        }
      }
    }
  ]
}
