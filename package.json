{
  "name": "sounds-deck",
  "private": true,
  "type": "module",
  "description": "Development tooling for the Sounds Deck Foundry VTT module. Foundry never reads this file.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "lint": "biome lint .",
    "format": "biome format --write .",
    "test": "node --test \"test/**/*.test.mjs\"",
    "check": "biome ci . && npm test && node tools/check-manifest.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14"
  }
}
